# Descoberta pré-autenticação — 19/08/2026 18:31 UTC

Fonte: `GET https://chamados.homyquimica.com.br/status.php` (endpoint público, sem token).
Nada foi escrito no GLPI.

| Item | Resposta do servidor | Leitura |
|---|---|---|
| GLPI | `OK` | aplicação de pé |
| Banco principal | `OK` | conexão e leitura funcionam; **não é** verificação de esquema |
| Réplicas de banco | `NO_DATA` | instância única, sem réplica |
| CAS | `NO_DATA` | sem SSO por CAS |
| LDAP | `NO_DATA` | **nenhum diretório LDAP/AD configurado** |
| IMAP | `NO_DATA` | sem IMAP |
| Coletores de e-mail | `NO_DATA` | **nenhuma caixa coletora criada** |
| Ações automáticas | `EM EXECUÇÃO: 0, TRAVADO: 0, TOTAL: 48` | 48 tarefas registradas, nenhuma travada |
| Sistema de arquivos | `OK` | diretório de sessão gravável |
| Plugins | `NO_DATA` | **nenhum plugin instalado/ativo** |

Outros fatos confirmados na mesma janela:

- `Server: Apache/2.4.58 (Ubuntu)` — bate com Ubuntu 24.04 LTS.
- Cookie de sessão emitido com `Secure`, `HttpOnly`, `SameSite=Lax`.
- Cabeçalhos `X-Content-Type-Options: nosniff`, `X-Frame-Options: SAMEORIGIN`,
  `Referrer-Policy: strict-origin-when-cross-origin` presentes.
- **Não há `Strict-Transport-Security`.** Vale acrescentar HSTS no Apache.
- API REST legada (`/apirest.php`) habilitada e exigindo `app_token`.
- Documentação da API respondendo em `/api.php/v1/`.
- Tema ativo declarado no HTML: `auror`; idioma `pt`.
- `status.php` responde a qualquer origem sem autenticação. É informação de
  infraestrutura exposta — avaliar restringir por IP no Apache.

## O que continua desconhecido sem token

Versão exata, integridade de esquema do banco, entidades, grupos, categorias ITIL,
localizações, calendários, feriados, SLA/SLM, regras de chamado, contagem de contas,
estado da conta de fábrica `glpi` e o modo (CLI ou web) das 48 ações automáticas.

A versão **não** pôde ser deduzida do `?v=` dos assets: o GLPI mistura uma semente
da instalação no hash, e nenhum `sha1`/`md5` de `10.0.x`/`11.0.x` bate com
`b98a979c5ab6d1bada8f3ad43b4db4fb96d3198f`.
